# GitHub-native orchestration as Phase-1 baseline

We choose GitHub-native orchestration as the baseline for Phase 1, using issue
labels, webhook events, and repository-native workflow controls as the primary
execution surface. We defer external multi-agent runtime frameworks to a later
phase because the first objective is deterministic pilot delivery with lower
operational complexity and faster governance hardening.
